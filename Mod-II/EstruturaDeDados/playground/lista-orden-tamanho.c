# include "listaencadeada.h"

main(){
	Lista i = no(3, no(4, NULL));
    Lista j = no(3, no(3, no(5, NULL)));
    Lista k = no(0, no(0, no(5, no(7, NULL))));
    
    printf(fmt, tam(i));
    printf(fmt,tam(j));
    printf(fmt,tam(k));
}
